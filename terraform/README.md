# Terraform

This directory is reserved for cloud-specific infrastructure modules. A typical production setup needs a managed PostgreSQL database, Redis, object storage, container runtime, TLS-enabled ingress, and private monitoring access.
